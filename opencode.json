{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": {
      "*": "allow",
      "git push": "ask",
      "git push *": "ask",
      "firebase deploy": "ask",
      "firebase deploy *": "ask",
      "npx firebase deploy": "ask",
      "npx firebase deploy *": "ask"
    }
  }
}
